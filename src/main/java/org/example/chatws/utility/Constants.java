package org.example.chatws.utility;

/**
 * Pensada para centralizar constantes que se repiten en varias partes del
 * backend (por ejemplo, los nombres de los destinos STOMP como
 * "/topic/public" o "/app", los valores de "tipo" del mensaje como
 * "CHAT"/"JOIN"/"LEAVE", etc.) en vez de tenerlos "hardcodeados" en
 * WebSocketConfig y ChatController.
 *
 * Por ahora esta vacia: todavia no se migraron esos valores hacia aca.
 */
public class Constants {

}
